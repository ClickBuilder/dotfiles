Проверка авторизации
Массив

```jsx
const username = 'Mux'

const isLoggedInTrue = true

const isLoggedInFalse = false


const App = () => {

let content

if (isLoggedInFalse){

content = <p>Hi, {username}</p>

}else{

content = <button>Log In</button>

}{/* Проверка авторизации*/}

  

const tasks = [

'1',

'2',

'3',

]

  

return (

<div>

<h1 className="Title">Rise</h1>

<p style={{color:'red', fontWeight: 700}}>Hi, {username}</p>

<p>{1+1}</p>

<p>{username.toUpperCase()}</p>

<p>{new Date().toLocaleDateString()}</p>

{/*Это комментарий внутри jsx */}

<hr/>

<label htmlFor="email">Email: </label>

<input id="email" type="email" required/>

{isLoggedInTrue && <p>Hi, {username}</p>}

{isLoggedInFalse && <p>Hi, {username}</p>}

<p>{isLoggedInFalse ? `Hi, ${username}!` : 'Please Log in'}</p> {/*Проверка авторизации*/}

{isLoggedInFalse ? <p>Hi, {username} </p> : <button>Log in</button>} {/*Проверка авторизации*/}

{content}

<ul>

{tasks.map((task) =><li key={task}>{task}</li>)}

</ul>

</div>

)

}

  

export default App
```